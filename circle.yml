machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install -U docker-compose==1.3.3

test:
  override:
    - make
    - sleep 10
    - make test
    - mkdir -p $CIRCLE_TEST_REPORTS/rspec
    - cp test/*.xml $CIRCLE_TEST_REPORTS/rspec/
    - docker images
